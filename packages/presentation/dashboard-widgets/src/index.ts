
export interface WidgetProps {
  title: string;
  data: any;
}

export const StatisticsWidget = ({ title, data }: WidgetProps) => {
  return null; // Implementation needed
};

export const ChartWidget = ({ title, data }: WidgetProps) => {
  return null; // Implementation needed
};
